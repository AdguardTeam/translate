name: Mirror

on:
  push:
    branches:
      - master

permissions:
  contents: read
  id-token: write

jobs:
  mirror:
    uses: AdGuardSoftwareLimited/actions/.github/workflows/mirror.yml@master
    with:
      team: extensions
      target_repo: git@github.com:AdguardTeam/translate.git
    secrets: inherit
